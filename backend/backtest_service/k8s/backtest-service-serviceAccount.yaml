# ServiceAccount for backtest-service
apiVersion: v1
kind: ServiceAccount
metadata:
  name: backtest-service-sa
  namespace: stock-management
automountServiceAccountToken: false  # 安全性考量，除非需要 k8s API 訪問